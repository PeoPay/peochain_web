<!DOCTYPE html>
<html>
<head>
  <title>PeoChain - Advanced Blockchain Architecture</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta charset="utf-8">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  
  <!-- This is a placeholder that will be replaced with the React app -->
  <div id="root"></div>
  
  <!-- In production, we would include the compiled React app here -->
  <% if Rails.env.production? %>
    <%= javascript_include_tag 'main' %>
  <% else %>
    <!-- In development, we would use the React dev server -->
    <script src="http://localhost:3000/main.js"></script>
  <% end %>
</head>
<body>
  <!-- Initial loading state before React mounts -->
  <div id="initial-loading" style="
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
    width: 100vw;
    font-family: sans-serif;
    background: linear-gradient(135deg, #fbf8f1 0%, #f5f1ea 100%);
  ">
    <div style="text-align: center;">
      <h1 style="color: #4a6fa5; margin-bottom: 20px;">PeoChain</h1>
      <div style="
        width: 50px;
        height: 50px;
        border: 5px solid #f3f3f3;
        border-top: 5px solid #4a6fa5;
        border-radius: 50%;
        margin: 0 auto;
        animation: spin 1s linear infinite;
      "></div>
      <style>
        @keyframes spin {
          0% { transform: rotate(0deg); }
          100% { transform: rotate(360deg); }
        }
      </style>
    </div>
  </div>
  
  <script>
    // Hide loading indicator when React mounts
    document.addEventListener('DOMContentLoaded', function() {
      const rootElement = document.getElementById('root');
      
      // Create a MutationObserver to watch for changes in the root element
      const observer = new MutationObserver(function(mutations) {
        if (rootElement.children.length > 0) {
          // When React mounts elements, hide the loading indicator
          document.getElementById('initial-loading').style.display = 'none';
          observer.disconnect();
        }
      });
      
      // Start observing the root element
      observer.observe(rootElement, { childList: true });
      
      // Set a timeout to hide the loader if React takes too long
      setTimeout(function() {
        document.getElementById('initial-loading').style.display = 'none';
      }, 5000);
    });
  </script>
</body>
</html>